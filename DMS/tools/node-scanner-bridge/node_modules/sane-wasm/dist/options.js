"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ScanOptions = exports.ScanOptionsMutable = exports.ScanOptionsBase = void 0;
const _1 = require(".");
/**
 * @private
 */
class ScanOptionsBase {
    [Symbol.iterator]() {
        return this.options[Symbol.iterator]();
    }
    static _getOpts(lib) {
        return __awaiter(this, void 0, void 0, function* () {
            const opts = [];
            let desc = null;
            for (let i = 0, n = -1; desc || i === 0; i++) {
                ({ option_descriptor: desc } = lib.sane_get_option_descriptor(i));
                if (desc) {
                    if (!desc.cap.INACTIVE && desc.cap.SOFT_DETECT && desc.type !== _1.SANEValueType.BUTTON) {
                        const { status, value } = yield lib.sane_control_option_get_value(i);
                        if (status !== _1.SANEStatus.GOOD) {
                            throw new Error(`Unexpected status ${_1.SANEStatus[status]} while getting option value.`);
                        }
                        opts.push({ index: i, descriptor: desc, value });
                        if (i === 0) {
                            n = value; // option 0 contains total number of options
                        }
                    }
                    else {
                        opts.push({ index: i, descriptor: desc, value: null });
                    }
                }
                else if (!desc && i !== n) {
                    throw new Error('Unexpected number of options.');
                }
            }
            return opts;
        });
    }
    constructor(lib, opts) {
        this.resolution = null;
        this.preview = null;
        this.tlX = null;
        this.tlY = null;
        this.brX = null;
        this.brY = null;
        this._lib = lib;
        this._setOpts(opts);
    }
    _setOpts(opts) {
        this.options = opts;
        this.zero = this.options[0];
        for (let i = 0; i < this.options.length; i++) {
            if (this.options[i].index !== i) {
                throw new Error(`Option index mismatch ${this.options[i].index} != ${i}.`);
            }
            switch (this.options[i].descriptor.name) {
                case 'resolution':
                    this.resolution = this.options[i];
                    break;
                case 'preview':
                    this.preview = this.options[i];
                    break;
                case 'tl-x':
                    this.tlX = this.options[i];
                    break;
                case 'tl-y':
                    this.tlY = this.options[i];
                    break;
                case 'br-x':
                    this.brX = this.options[i];
                    break;
                case 'br-y':
                    this.brY = this.options[i];
                    break;
            }
        }
    }
    _setValue(index, value) {
        return __awaiter(this, void 0, void 0, function* () {
            const auto = value === null; // null means auto
            const { status, info } = auto ? yield this._lib.sane_control_option_set_auto(index) : yield this._lib.sane_control_option_set_value(index, value);
            if (status !== _1.SANEStatus.GOOD) {
                return { status, info, opts: null };
            }
            if (info.RELOAD_OPTIONS) {
                return { status, info, opts: yield ScanOptionsBase._getOpts(this._lib) };
            }
            if (info.INEXACT || typeof value === 'number' || auto) {
                // auto triggers get value... ok
                // number triggers get value... this is a quirk for fixed numbers
                // because the conversion from floating to fixed point happens
                // on the library side, we might get INEXACT == false but in
                // reality there were some small difference
                // XXX: this could probably be fixed on the C side
                const { status, value: v } = yield this._lib.sane_control_option_get_value(index);
                if (status !== _1.SANEStatus.GOOD) {
                    throw new Error(`Unexpected status ${_1.SANEStatus[status]} while getting option value.`);
                }
                value = v;
            }
            // update value
            const opts = [...this.options];
            opts[index] = Object.assign(Object.assign({}, opts[index]), { value });
            return { status, info, opts };
        });
    }
}
exports.ScanOptionsBase = ScanOptionsBase;
/**
 * Helper class for getting/setting scan options.
 *
 * Any changes (i.e. calls to {@link ScanOptionsMutable.setValue}) affect the
 * internal state.
 */
class ScanOptionsMutable extends ScanOptionsBase {
    static get(lib) {
        return __awaiter(this, void 0, void 0, function* () {
            return new this(lib, yield this._getOpts(lib));
        });
    }
    setValue(index, value) {
        return __awaiter(this, void 0, void 0, function* () {
            const { status, info, opts } = yield this._setValue(index, value);
            if (status == _1.SANEStatus.GOOD) {
                this._setOpts(opts);
                return { status, info };
            }
            return { status, info };
        });
    }
}
exports.ScanOptionsMutable = ScanOptionsMutable;
/**
 * Helper class for getting/setting scan options.
 *
 * Any changes (i.e. calls to {@link ScanOptions.setValue}) return a
 * new class instance.
 */
class ScanOptions extends ScanOptionsBase {
    static get(lib) {
        return __awaiter(this, void 0, void 0, function* () {
            return new this(lib, yield this._getOpts(lib));
        });
    }
    setValue(index, value) {
        return __awaiter(this, void 0, void 0, function* () {
            const { status, info, opts } = yield this._setValue(index, value);
            if (status == _1.SANEStatus.GOOD) {
                return { status, info, updated: new ScanOptions(this._lib, opts) };
            }
            return { status, info, updated: null };
        });
    }
}
exports.ScanOptions = ScanOptions;
