{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Full Build (backend+frontend)",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-ExecutionPolicy",
				"Bypass",
				"-Command",
				"cd backend; ./mvnw -B clean verify; cd ../frontend; npm run build"
			],
			"isBackground": false
		}
	]
}